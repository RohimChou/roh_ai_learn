# 接續點: 第 6 階 y = 3x² + 2x + 1

> 上一階 (第 5 階 y=x²) 完成 ✅ code 在 `26_03_x_squared/`, 筆記在 wiki learn_ai/03。
> 這階照舊在 `0_clean/` 開工, 做完再搬去 `26_04_polynomial/`。

## 這階要幹嘛

一顆 neuron、**兩個 feature**: `[x², x]`。是 26_02 (多輸入) + 26_03 (換 feature) 的合體。

1. **開工前先寫下預測**: 收斂後 `weights[0]`、`weights[1]`、`bias` 各是多少? (跟公式對)
2. 資料自己生: x 挑 -3~3 之間 8 筆左右, y 用公式算
3. 更新式照 26_02 的規則: 每個 weight 乘**自己對應的那個輸入**
4. 順路做原第 4 階的事: weight 更新從逐行改成 **for 迴圈** (第 7 階 hidden layer 會需要)

## ⚠️ 地雷預告

這階埋了一個會炸的坑, 跟 learning rate 有關。炸了**不要急著修**, 先觀察:
**哪個參數先炸? 為什麼是它?** — 那才是這階真正要學的東西。

(別偷看 `EXERCISES.md` 的「第 5、6 階的重點」那節, 答案劇透在裡面)
